@page
@using BudgetRequests.Data
@model BudgetRequests.Pages.Organizations.DetailsModel

@{
    ViewData["Title"] = "Details";
}

@*
<h1>Details</h1>

<div>
    <h4>Organization</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Organization.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Organization.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Organization.IsStudentCouncil)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Organization.IsStudentCouncil)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Organization.GrossBudget)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Organization.GrossBudget)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Organization?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
*@
<div class="create-container">
    <div class="profile-card">
    
        <div class="text-data">
            <span class="name">@Model.Organization.Name</span>
        </div>
    
        <div class="role">
            Adviser <span class="name">@Model.Organization.Adviser.FullName</span>
        </div>
        
        @foreach (var officerRole in Model.OfficerRoles.ToList())
        {
            <div class="role">
                @officerRole.Position.GetText() <span class="name">@officerRole.Officer.FullName</span>
            </div>
        }

        <div class="field">
            <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@Model.Organization.Id">Edit Organization</a>
        </div>
        
        <div class="field">
            <a class="btn btn-primary" asp-page="./Index">Go Back</a>
        </div>
    
    </div>
</div>
<script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
<script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>